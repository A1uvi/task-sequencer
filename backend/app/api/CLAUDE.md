# app/api — API Package

## Purpose
Contains all FastAPI routers and route handlers.

## Structure
- `routes/` — one file per resource domain

## Router Registration
All routers in `routes/` are imported and registered in `backend/main.py`.
