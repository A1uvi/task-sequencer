# src/components/layout — Layout Shell Components

## Components
- `Layout.tsx` — wraps pages with sidebar and topbar
- `Sidebar.tsx` — navigation links
- `TopBar.tsx` — user info, logout, search shortcut

## Usage
All pages render inside `<Layout>` which is included in the route wrapper.
